# 基本配置
[BASE]
# 端口
PORT=9201
# 初始化最大连接数
MAXCLIENT=1000
# 处理线程数目, 一般为cpu内核数目
MAXHANDLETHREAD=8

# 日志配置
[LOG]
# 日志级别(0-5, 0:关闭, 5:全开)
LOGLEVEL=3
# 日志路径
LOGDIR=log/camshare_executor
# 调试模式(0:关闭, 1:开启)
DEBUGMODE=0